<%= form_for(@convenience_item) do |f| %>
  <% if @convenience_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@convenience_item.errors.count, "error") %> prohibited this convenience_item from being saved:</h2>

      <ul>
      <% @convenience_item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
